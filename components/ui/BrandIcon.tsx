/**
 * Secondary mark, used in the footer. The dimensional app icon from
 * the treatments board: cream rim, paper face, Signal Blue script.
 * The primary lockup (tile + “Websites”) stays on the nav.
 */
export default function BrandIcon() {
  return (
    <span className="brand-icon" aria-hidden="true">
      <span className="brand-icon__face">
        <span className="brand-icon__glyph" />
      </span>
    </span>
  )
}
