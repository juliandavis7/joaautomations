# Dependencies
node_modules
.pnp
.pnp.js
# pnpm content-addressable store when `store-dir` is inside the repo (large; never commit)
.pnpm-store/

# Next.js
.next/
out/
build
dist
*.tsbuildinfo
# Generated by `next` (do not commit; regenerated on dev/build)
next-env.d.ts

# Vercel
.vercel

# Environment secrets. Keep .env.example committed if you add one.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log

# Testing / coverage
coverage
.nyc_output

# Tools / caches
.eslintcache
.turbo
.prettiercache
*.pid
*.seed
*.tgz

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editors
.idea
*.swp
*.swo
*~

# Local AI / assistant (optional local config)
.claude

# One-off Pillow venv used to rasterize icons (prefer sharp in /tmp)
.venv-sign-icons/

# Direction worktrees live beside the repo, not inside it
/worktrees/
